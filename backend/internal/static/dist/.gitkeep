# Placeholder so `//go:embed all:dist` compiles even before the frontend has
# been built. The Makefile target `make build` populates this directory with
# the SvelteKit static export before running `go build -tags=embed`.
