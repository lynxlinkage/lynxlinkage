services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lynxlinkage
      POSTGRES_USER: lynxlinkage
      POSTGRES_PASSWORD: lynxlinkage
    ports:
      - "5432:5432"
    volumes:
      - lynxlinkage_dev_data:/var/lib/postgresql/data

volumes:
  lynxlinkage_dev_data:
