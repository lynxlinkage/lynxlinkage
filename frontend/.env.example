# Backend URL used by SvelteKit's server-side load functions during
# `pnpm dev` (prerender) and `pnpm build`. In dev the Vite proxy in
# vite.config.ts sends client-side /api requests to the same target.
BACKEND_URL=http://localhost:8080
